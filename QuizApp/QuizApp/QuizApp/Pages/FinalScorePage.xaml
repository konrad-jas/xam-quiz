<?xml version="1.0" encoding="UTF-8"?>
<pages:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:QuizApp.Core.Pages;assembly=QuizApp"
             x:Class="QuizApp.Core.Pages.FinalScorePage">
	<StackLayout HorizontalOptions="Center" VerticalOptions="Center">
		<StackLayout IsVisible="{Binding MadeItToHighscores}">
			<Label Text="Congratulations!"/>
			<Label Text="{Binding Place, StringFormat='You made it to {0} place in the highscores!'}"/>
		</StackLayout>
		<Label Text="{Binding Score, StringFormat='Your final score is {0}'}" FontSize="20" FontAttributes="Bold"/>
		<Button Text="Try again" Command="{Binding RestartCommand}"/>
		<Button Text="Back to menu" Command="{Binding MenuCommand}"/>
	</StackLayout>
</pages:BasePage>

